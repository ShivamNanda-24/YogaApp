<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Button in checked/selected state -->

    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="#007FFF" /> <!-- Blue color for checked state -->
        </shape>
    </item>

    <!-- Button in default state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="#FFFFFF" /> <!-- White color for default state -->
            <stroke android:width="1dp" android:color="#000000" /> <!-- Optional border -->
        </shape>
    </item>
</selector>
